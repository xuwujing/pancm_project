insert into t_arp(id,ip_addr,mac_addr,sw_ip,open_port,
ip_status,check_last_time,company,device_type,model_level,
model_des,online_time,arp_out_id,arp_out_index,arp_out_desc,
deal_last_time,deal_last_mark,cur_mac_addr,cur_company,cur_open_port,
cur_arp_out_index,cur_arp_out_desc,cur_model,cur_model_des,dis_status,
alive,alive_last_time,onvif_addr,mute,task6_last_time,
server_os,vlan,area_id,mac_out,check_open_port,
open_port_all,serial_no,version,mac_scan,model_des_change,
mac_change,arp_out_change,open_port_change,mac_addr_block)
select ID,ipAddr,macAddr,swIP,openPort,
ipStatus,checkLastTime,company,model,modelLevel,
modelDes,onlineTime,arpOutID,arpOutIndex,arpOutDesc,
dealLastTime,dealLastMark,curMacAddr,curCompany,curOpenPort,
curArpOutIndex,curArpOutDesc,curModel,curModelDes,isDistributed,
isAlive,aliveLastTime,onvifXAddr,isMute,task6LastTime,
serverOS,vlan,areaId,macOut,checkOpenPort,
openPort1,SerialNo,Version,macScan,modelDesChange,
macChange,arpOutChange,openPortChange,macAddrBlock
from guard_05.t_arp;

insert into t_deny(id,arp_id,ip_addr,area_id,sw_ip,
arp_out_id,old_arp_out_id,from_int_index,mac_addr,old_mac_addr,
open_port,old_open_port,model_des,old_model_des,deny_time,
deny_reason,deny_reason_int,deal_status,deal_time,deal_mark,enable
)
select ID,arpId,ipAddr,areaId,swIP,
arpoutId,oldArpoutId,fromIntIndex,macAddr,oldMacAddr,
openPortList,oldOpenPortList,modelDes,oldModelDes,denyTime,
denyReason,denyReasonInt,isDealed,dealTime,dealMark,1
from guard_05.t_deny;

=================
CREATE TABLE `t_arp` (
  `` int(4) NOT NULL AUTO_INCREMENT,
  `` varchar(20) DEFAULT NULL,
  `` varchar(20) DEFAULT NULL,
  `` varchar(20) DEFAULT NULL,
  `openPortList` varchar(1000) DEFAULT NULL,
  `` varchar(1000) DEFAULT NULL COMMENT '开放端口列表',
  `` tinyint(1) DEFAULT '0' COMMENT '0:初始;2:已拒绝;99:白名单',
  `` varchar(30) DEFAULT NULL COMMENT '最后扫描时间',
  `` varchar(100) DEFAULT NULL COMMENT '厂商',
  `deviceType` tinyint(1) DEFAULT NULL COMMENT '设备类型',
  `` tinyint(1) DEFAULT '0' COMMENT '设备类型',
  `` tinyint(1) DEFAULT '0' COMMENT '设备识别等级 0:未识别；1:协议扫描识别; 2:端口扫描识别;',
  `` varchar(50) DEFAULT NULL,
  `` varchar(30) DEFAULT NULL COMMENT '上线时间',
  `` int(4) DEFAULT '0' COMMENT 't_arpout表中的主键',
  `` int(4) DEFAULT '-1' COMMENT '学习到的物理端口编号',
  `` varchar(40) DEFAULT NULL COMMENT '学习到的物理端口',
  `` varchar(30) DEFAULT NULL COMMENT '最后处置时间',
  `` varchar(255) DEFAULT NULL COMMENT '最后一次的处理意见',
  `` varchar(20) DEFAULT NULL COMMENT '当前MAC地址',
  `` varchar(100) DEFAULT NULL COMMENT '当前公司名称',
  `curOpenPortList` varchar(255) DEFAULT NULL,
  `` varchar(255) DEFAULT NULL COMMENT '当前开发端口',
  `` int(4) DEFAULT '-1',
  `` varchar(40) DEFAULT NULL,
  `` tinyint(1) DEFAULT '0' COMMENT '当前设备类别',
  `` varchar(50) DEFAULT NULL COMMENT '如果设备型号有改变，记录新的设备信号',
  `` tinyint(1) DEFAULT '0' COMMENT '该ip是否是已经分配的IP 1：已分配；2：未分配；3:未分配但是被放行',
  `` tinyint(1) DEFAULT '2' COMMENT 'IP是否在线;1:在线；2；不在线',
  `` varchar(30) DEFAULT NULL COMMENT '最后在线时间',
  `` varchar(500) DEFAULT NULL COMMENT 'onvif服务地址(摄像机才有)',
  `` tinyint(1) DEFAULT '0' COMMENT '是否哑终端 0：活跃终端；1：哑终端',
  `` varchar(30) DEFAULT NULL COMMENT '测试任务6的最后时间',
  `` varchar(255) DEFAULT NULL COMMENT '服务器操作系统版本',
  `` int(4) DEFAULT '-1' COMMENT 'vlan号',
  `` int(4) DEFAULT '-1' COMMENT '地区代码',
  `` int(4) DEFAULT '-1' COMMENT 'MAC对应的临时端口号(12800系列)',
  `` tinyint(1) DEFAULT '0' COMMENT '是否检测过  0：未检测 1：已经检测',
  `openPortList1` varchar(1000) DEFAULT NULL,
  `` varchar(1000) DEFAULT NULL,
  `` varchar(50) DEFAULT NULL COMMENT '设备序列号',
  `` varchar(50) DEFAULT NULL COMMENT '软件版本',
  `` varchar(20) DEFAULT NULL COMMENT '协议扫描到的MAC地址',
  `` tinyint(1) DEFAULT '0' COMMENT '设备型号是否改变 0：未改变；1：改变',
  `` tinyint(1) DEFAULT '0' COMMENT 'MAC地址是否改变 0：未改变；1：改变',
  `` tinyint(1) DEFAULT '0' COMMENT '入网路径是否改变 0：未改变；1：改变',
  `` tinyint(1) DEFAULT '0' COMMENT '开放端口是否改变 0：未改变；1：改变',
  `` varchar(20) DEFAULT NULL COMMENT '被阻断了的MAC地址',
  PRIMARY KEY (`ID`),
  KEY `idx_mac` (`macAddr`)
) ENGINE=MyISAM AUTO_INCREMENT=178984 DEFAULT CHARSET=utf8

